---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: inputs.template.fn.crossplane.io
spec:
  group: template.fn.crossplane.io
  names:
    categories:
    - crossplane
    kind: Input
    listKind: InputList
    plural: inputs
    singular: input
  scope: Namespaced
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: input can be used to provide input to this function.
        properties:
          apiversion:
            description: |-
              apiversion defines the versioned schema of this representation of an object.
              servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              more info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          example:
            description: example is an example field. replace it with whatever input
              you need. :)
            type: string
          token:
            description: token is a field that provides the gitlab-api-key.
            type: string
          baseurl:
            description: baseurl provides the base-url to the gitlab instance.
            type: string
          kind:
            description: |-
              kind is a string value representing the rest resource this object represents.
              servers may infer this from the endpoint the client submits requests to.
              cannot be updated.
              in camelcase.
              more info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
        type: object
    served: true
    storage: true
